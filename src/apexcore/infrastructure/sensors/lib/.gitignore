# P0.8 (релиз v0.5.1): DLL и .sys для LibreHardwareMonitorLib теперь
# коммитятся в git — для UX «работает из коробки» (см. план §P0.8,
# согласованный вариант 3). MPL-2.0 / OpenLibSys-BSD это разрешают
# (см. NOTICE.md).
#
# Размер ~3 МБ за всю кучу — приемлемо для one-time коммита.
# fetch_lhm.ps1 остаётся идемпотентным скриптом обновлений.
#
# Что НЕ коммитим:
# - *.pdb — debug-символы, не нужны в release.
# - *.zip — кеш fetch_lhm.ps1.
*.pdb
*.zip
