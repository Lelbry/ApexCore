"""Опциональный Web UI на FastAPI (фаза 2, Windows).

Доступен только при установленном extras-наборе ``webui``:
    pip install -e ".[webui]"

Запуск (после установки):
    apexcore webui --host 127.0.0.1 --port 8765
"""
