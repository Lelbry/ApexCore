# Placeholder: scripts/build_windows.ps1 копирует сюда HTML/CSS/JS из
# new-app/src/apexcore/interfaces/webui/static/setup/ перед dotnet publish.
# Сами файлы — в git под `static/setup/`, единый source of truth.
